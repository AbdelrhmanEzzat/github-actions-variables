name: SSH Working Deploy

on:
  push:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Manual SSH to test connection
        run: |
          echo "${{ secrets.key }}" > key.pem
          chmod 600 key.pem
          ssh -o StrictHostKeyChecking=no -o IdentitiesOnly=yes -i key.pem ubuntu@195.15.194.78 "echo ✅ SUCCESS: Connected to remote VM"
